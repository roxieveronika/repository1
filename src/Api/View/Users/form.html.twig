{% extends 'Api/View/Layout/layout.html.twig' %}

{% block content %}

    <form action="{{ url('userSave', {id : result.id}) }}" method="post" class="navbar-form navbar-left" role="search">
        <div class="form-group">
            <input type="text" name="login" class="form-control" placeholder="login" value="{{ result.login }}">
        </div>
        <div class="form-group">
            <input type="text" name="haslo" class="form-control" placeholder="hasło" value="{{ result.haslo }}">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
    </form>

{% endblock %}