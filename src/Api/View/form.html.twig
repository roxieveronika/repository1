{% extends 'Api/View/Layout/layout.html.twig' %}

{% block content %}

    <form action="{{ url('categorySave', {id : result.id}) }}" method="post" class="navbar-form navbar-left" role="search">
        <div class="form-group">
            <input type="text" name="nazwa" class="form-control" placeholder="Name" value="{{ result.nazwa }}">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
    </form>

{% endblock %}